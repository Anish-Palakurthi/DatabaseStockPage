push all to git?
why is require not working?
https://www.youtube.com/watch?v=8nKyNzXAjP8

//formats and creates Chart and then makes it visible
            function createChart(combinedArray) {
                let myChart = document.getElementById("myChart").getContext("2d");
                new Chart(document.getElementById("myChart"), {
                    type: 'line',
                    data: {
                        labels: dates,
                        datasets: [
                            {
                                label: "Closing Prices for " + inputS1,
                                data: closes1,
                                borderColor: "#009900"
                            },
                            {
                                label: "Closing Prices for " + inputS2,
                                data: closes2,
                                borderColor: "#0066CC"

                            }


                        ]
                    },
                    options: {
                        legend: { display: false },
                        title: {
                            display: true,
                            text: "Price of Share ($) for " + inputS1 + "(Green) vs " + inputS2 + " (Blue)"

                        }
                    }
                });
            }



        }